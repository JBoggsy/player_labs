# Vision and goals

Recorded from the user's design prompt. This document states *why* the project
exists and *what* its major parts are. The other design documents detail each part.

## The problem being solved

Today the lab is a set of directories, skills, and markdown files that a coding
agent works through in one terminal session at a time. The human sees the agent's
knowledge, beliefs, and actions only by reading files or watching a session. Long
running work (monitoring an experiment, maintaining knowledge, tracking the field)
has to be babysat, and only one thing happens at a time.

The Player Agentic Development Environment (Player-ADE) upgrades this into a
web-UI-based environment where a human and a system of coding agents develop game
policies together, with many agents working in parallel over long horizons.

## Three aims

1. **Significantly improve the user's experience** of developing and improving
   policies with coding agents.
2. **Upgrade the existing general-purpose, game-agnostic tools, skills, and
   knowledge** in the lab.
3. **Enable parallel, long-running agentic tasks**, providing both the harness that
   runs them and the UI/UX the user needs to interact with them.

## The core idea: collaboration between user and player

The user and the player (the agent system) collaborate. For that to work:

- The player needs the skills, knowledge, and tools for policy development *and*
  for interacting with the user.
- The user needs observability into the player's knowledge, actions, beliefs, and
  strategies, and needs to be able to both direct the player and respond to the
  player's needs, actions, and questions.

## Major components

Each has its own design document.

| Component | One-line description | Document |
| --- | --- | --- |
| Collaborative wiki | Wiki-like store of accumulated knowledge that both user and player read and edit, with each side's edits visible to the other | [01](01-collaborative-wiki.md) |
| Idea/experiment lifecycle | Ideas → hypotheses → experiments → results, generated by either side, executed and recorded by the player, visible and commentable by the user | [02](02-experiment-lifecycle.md) |
| Skills and tools | Everything the player needs: generating ideas, planning and running experiments, running tests, talking to Softmax, doing research | [03](03-player-system-harness.md) |
| Specialized agents | Separate agents or sub-agents, each with its own harness or at least its own instruction file, for specific jobs | [04](04-agents.md) |
| Direct interaction | The user works with multiple instances of the player in two modes: short interactive sessions, and long-running loops (a repeating task or a long-horizon goal) | [03](03-player-system-harness.md) |
| Per-coworld labs | Coworld-specific knowledge stays separate; coworld-agnostic knowledge is unified. This mirrors the current directory layout and must be mirrored in the UI | [01](01-collaborative-wiki.md), [03](03-player-system-harness.md) |
| Softmax integration | Tight integration via tooling and UI: easy querying and downloading of episodes, replays, logs; easy replay viewing, likely with coworld-specific parts | [03](03-player-system-harness.md) |

## Principle: natural-language strategy is the core of a policy

The user wants the new system to enforce, or at least enable, a discipline where a
policy's strategy is written in natural language in markdown files, and the code
implementation is derived from that written strategy. Changes to a strategy file
must directly result in corresponding changes to the policy code. This is detailed
in the wiki document and raises questions in [Q7 and Q8](05-open-questions.md).

## What this project is not deciding yet

The agent harness (how many agents, how they are scheduled, how they talk) is
deliberately under-specified. The user's instruction is to first synthesize the
current state of the art and proven solutions (paperclip, gastown, Metta-AI's
`cogamer` and `co-gas`, research papers), then customize for this project together.
See the [research plan](07-research-plan.md).
