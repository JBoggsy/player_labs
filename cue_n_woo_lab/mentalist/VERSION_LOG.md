# mentalist — version log

Each uploaded policy version → the change it carries (lab best practice; the
canonical id is the policy-version UUID from `policy_lifecycle.py versions`).

| Version | Change | Notes |
|---|---|---|
| v1 (`9fcac03b-a8b8-4195-8402-7c887a7574c0`) | Initial player: 3 fixed private questions → TF-IDF style classifier (61 styles × 2 draws library) → Bedrock Claude (`us.anthropic.claude-opus-4-8`) writes in-style proposals + blind answers; deterministic fallbacks on Bedrock failure/low clock. | Uploaded + submitted to the Cue N Woo league 2026-06-12 (`--run python --run=-m --run mentalist --use-bedrock`). First submission disqualified by a league-side IAM bug (see `../docs/league-infra-incident-2026-06-12.md`); resubmitted after the hotfix. **Bedrock was broken in every hosted episode** (marketplace AccessDenied → silent fallback); the stale upload predates the metta#15616 player-pod secret-env fix. |
| v2 (`13ce7daa-406a-49a5-a839-0fd2243a2df9`) | **Dual-backend LLM writer.** `writer.py` now talks to Claude through the `anthropic` SDK over a runtime-selected backend — `USE_BEDROCK` → `AnthropicBedrock`, else `ANTHROPIC_API_KEY` → `Anthropic` (crewrift/suspectra convention) — replacing raw boto3 `converse`. Fixes the production fallback two ways: re-uploading re-stores the secret-env under the current dispatch path (metta#15616), and the direct-API path is infra-independent. Adds the Anthropic-API model id (`claude-opus-4-8`, no `us.` prefix) + dual-backend config knobs; `BedrockWriter` kept as alias. | Gate-1 verified 2026-06-13: real-config local episode vs the live worker with `--use-bedrock --aws-profile softmax` → log `LLM backend: bedrock ... ok`, genuine in-style Claude prose (pirate style, all answers ≤12 tokens, no rejections, no fallback), scores [579.2, 80.8], 86s. 29 unit tests pass. Upload with `--use-bedrock` (and optionally `--secret-env ANTHROPIC_API_KEY=...`). |
