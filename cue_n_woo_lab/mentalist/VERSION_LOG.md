# mentalist — version log

Each uploaded policy version → the change it carries (lab best practice; the
canonical id is the policy-version UUID from `policy_lifecycle.py versions`).

| Version | Change | Notes |
|---|---|---|
| v1 (`9fcac03b-a8b8-4195-8402-7c887a7574c0`) | Initial player: 3 fixed private questions → TF-IDF style classifier (61 styles × 2 draws library) → Bedrock Claude (`us.anthropic.claude-opus-4-8`) writes in-style proposals + blind answers; deterministic fallbacks on Bedrock failure/low clock. | Uploaded + submitted to the Cue N Woo league 2026-06-12 (`--run python --run=-m --run mentalist --use-bedrock`). First submission disqualified by a league-side IAM bug (see `../docs/league-infra-incident-2026-06-12.md`); resubmitted after the hotfix. **Bedrock was broken in every hosted episode** (marketplace AccessDenied → silent fallback); the stale upload predates the metta#15616 player-pod secret-env fix. |
| v2 (`13ce7daa-406a-49a5-a839-0fd2243a2df9`) | **Dual-backend LLM writer** (still on opus-4-8). `writer.py` talks to Claude through the `anthropic` SDK over a runtime-selected backend — `USE_BEDROCK` → `AnthropicBedrock`, else `ANTHROPIC_API_KEY` → `Anthropic` (crewrift convention) — replacing raw boto3 `converse`. Dual-backend config knobs added; `BedrockWriter` kept as alias. | Gate-1 (local, softmax profile) passed with real prose — BUT the re-upload theory was **WRONG**: v2's first league qualifier (job aaeca506) **still threw the marketplace 403 on opus-4-8** → fallback. Re-uploading did not fix Bedrock; the episode-runner role lacks opus-4-8 access. Superseded by v3. |
| v3 (`ec18c7d6-12f1-4592-af98-a7be29ea22fd`) | **Switch Bedrock model opus-4-8 → haiku-4-5** (`us.anthropic.claude-haiku-4-5-20251001-v1:0`; direct-API id `claude-haiku-4-5-20251001`). Root cause of the league fallback was the MODEL, not the upload or the writer: the episode-runner IRSA role can invoke haiku-4-5 (crewrift/crewborg uses it over the same `--use-bedrock` path and works) but NOT opus-4-8 (marketplace 403). Same role/region; only the model differs. Trade-off: haiku < opus on answer quality; revisit if opus-4-8 gets subscribed for the role. | Gate-1 verified 2026-06-13: real-config local episode vs the live worker → `LLM backend: bedrock, model us.anthropic.claude-haiku-4-5-20251001-v1:0 ... ok`, real in-style pirate prose, all ≤12 tokens, no fallback, status complete. 29 unit tests pass. **League validation = the real proof** (local runs use the softmax profile, which CAN reach opus; only the tournament role can't). |
