# Exact deployed accelerated-wow 0.1.146 game image. It supplies the canonical
# environment/ and player.sdk Python contracts copied into wowborg's policy image.
# Canonical live Coworld: cow_ff82f1c4-d1f5-4291-810e-039e67ac8173
#
# Keep this in step with the version being built: v60 shipped against 0.1.127
# (`sha256:7262b629…`) via a `--base` override that was never recorded here, so the
# repo could not reproduce it from this file alone. Bump this pin when a version
# ships against a new release.
WOWBORG_ENVIRONMENT_IMAGE=public.ecr.aws/q5f4m8t9/cogames@sha256:ab5f989cbdde51c5ae3ca80550365798f780c483927cb54a19c1c624632be01c
